import type { NextPage } from 'next';
import styled from '@emotion/styled';
const Home: NextPage = () => {
  return (
    <Container>
      <span>초기화면</span>
    </Container>
  );
};

export default Home;

const Container = styled.div``;
